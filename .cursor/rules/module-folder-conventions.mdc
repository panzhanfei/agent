---
description: 模块目录约定 — index.ts 聚合导出、interface.ts 放类型、导入路径
alwaysApply: true
---

# 模块目录约定（index / interface / 导入路径）

适用于 `apps/brain-service`、`packages/*` 等 TypeScript 模块目录（有独立职责的文件夹）。

## 每个职责文件夹应有

| 文件 | 职责 |
|------|------|
| `index.ts` | **聚合导出**本目录对外 API（函数、常量、类型）；单节点 Agent 的 LangGraph 主流程也写在此 |
| `interface.ts` | **类型约束**（`type` / `interface`）；不含业务实现 |

实现文件（`*-guard.ts`、`retrieve.ts` 等）只写逻辑，类型从 `./interface` 引入。

## 导入规则

```ts
// ✅ 同级（同一文件夹内）
import type { RoutedIntakeDecision } from "./interface";
import { applyIntakeChitchatGuard } from "./intake-chitchat-guard";

// ✅ 跨文件夹：走目标目录的 index，用 @ 别名
import {
  applyIntakeChitchatGuard,
  type RoutedIntakeDecision,
} from "@/agentflow/agents/online/intake-coordinator/guards";

// ✅ 仅类型、且要避开 barrel 循环依赖时：可直接 import type 自 interface
import type { CompositeRetrievalSlot } from "@/agentflow/agents/online/intake-coordinator/composite/interface";

// ❌ 禁止：跨目录深挖具体实现文件
import { applyIntakeChitchatGuard } from "@/agentflow/.../guards/intake-chitchat-guard";
```

父模块 `index.ts` 再聚合子目录；包外优先用模块根入口（如 `@/.../intake-coordinator`）。

## LangGraph 图节点（`runXxxNode`）

| 包类型 | 图节点出口 | 业务逻辑 |
|--------|-----------|----------|
| **单节点 Agent** | 包根 `index.ts`（如 `corpus-lister/index.ts` 导出 `runListRetrieverNode`） | 分类子目录（`list/`、`slot/` 等） |
| **多节点 / 编排包** | 各子能力 `子目录/index.ts`；包根 `index.ts` **只聚合** | 同左，由子目录 `index.ts` 调用 helper |
| **纯库包** | 无图节点（如 `knowledge-manager` 的 recall/composite；有图节点时写在包根） | 按职责分子目录 |

**`index.ts` 排版：** 先 `import` + barrel `export { … } from "./…"`；**LangGraph 节点实现（`runXxxNode`）放在文件最下方**。纯节点子目录（仅一个 `runXxxNode`）可只有 import + 底部实现。

```text
corpus-lister/                    plan-fanout/                         sideline/file/
├── index.ts    ← runListRetrieverNode + barrel   ├── index.ts  ← 只聚合     ├── index.ts  ← 只聚合
├── interface.ts                                  ├── interface.ts           ├── interface.ts
├── list/                                         ├── fan-out/index.ts       ├── graph/compile.ts  ← 平级图
└── slot/                                         ├── plan-slot-join/        ├── workspace/
                                                  ├── plan-slot-post/        ├── save-hitl/
                                                  └── plan-merge/            └── vault/

knowledge-manager/
├── index.ts    ← runKmRetrieveNode + barrel
├── slot/
├── recall/
└── composite/
```

```ts
// ✅ compile.ts：从各 Agent 引节点，从 plan-fanout 引编排
import { runKmRetrieveNode } from "@/agentflow/agents/online/knowledge-manager";
import { runListRetrieveNode } from "@/agentflow/agents/online/corpus-lister";
import { runPlanSlotJoinNode, fanOutPlanWorkers } from "@/agentflow/agents/online/plan-fanout";

// ❌ 禁止 plan-fanout 代管 KM / Lister 的 retrieve 节点
// ❌ 禁止 node/ 子目录（已废弃）
```

**PlanFanOut 边界：** 只拥有 `fan-out` → `planSlotJoin` → `planSlotPost` → `planMerge`；`kmRetrieve`→KM；`listRetrieve`→Lister；`memRetrieve`→user-fact；`toolRetrieve`→tool-orchestrator；`planDag`→dag-executor；`summarizeSlot`→content-summarizer；`runUserFactSideNode`→user-fact/side。**不拥有** vault / 文件 HITL（→ `agents/sideline/file`，主图只 `fileHandoff`）。

**平级子线（sideline，不是 nested subgraph）：** `agents/sideline/<line>/` 各自 compile；runtime 在主图 END 后调用。禁止 `parent.addNode(compiledSubgraph)`。主图节点名不得与 state channel 同名（信封 channel 用 `fileEnvelope`，节点用 `fileHandoff`）。

## 新建 / 改动检查

1. 新类型 → 放进该目录 `interface.ts`，并由 `index.ts` re-export
2. 新公开函数 → 实现文件 + `index.ts` 导出
3. 同级 `./`，跨目录 `@.../<folder>`（不要 `.../具体文件名`）
4. `interface.ts` 只依赖其它目录的类型（优先 `.../interface`），避免与实现 barrel 循环
5. 新图节点 → 单节点写包根 `index.ts`；多节点写 `子目录/index.ts`；**不建** `node/`
6. 子模块 import 禁止走包根 barrel（避免循环依赖）
